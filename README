lit gem sample App
========================

Sample app to test lit gem in action - should include many popular gems (ie. devise,
simple_form, formtastic, kaminari, activeadmin) to properly test LIT in action.
App is working with redis as key value storage engine and on 2 unicorn workers
to prove it works and caches values properly in production environment.


________________________

App environment:
Rails: 3.2.9
Ruby: 1.9.3
